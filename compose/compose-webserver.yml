version: '3.8'
services:
  webserver:
    image: docker.io/library/nginx:alpine 
    container_name: local-homepage
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped
